"""scanfix v1 — local, deterministic: mesh in → restored + print-ready + honest report.

Pipeline: load → analyze → repair → idealize (prismatic only, deviation-gated)
→ printability → deviation certificate → report.

No models, no network, no silent edits: every change is logged in the report.
"""

__version__ = "1.0.0"
